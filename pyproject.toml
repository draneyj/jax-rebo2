[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

# Project metadata is intentionally defined in setup.py because the
# `interpax` dependency is a vendored git submodule referenced by an
# absolute file:// URL that must be computed at install time.
